export interface DashboardCount {
  for_release_loan_count?: number;
  loan_released_count?: number;
  member_count?: number;
  overdue_loan_count?: number;
  new_registered_since_last_week?: number;
}
